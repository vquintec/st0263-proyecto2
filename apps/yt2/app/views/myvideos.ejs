<% include header %>

<script>
  function deleteVideo(id) {

    url = "http://" + <%- ip %> + ":3003" + <%- baseUrl %> + "videos/delete/" + id;
    alert(ip);
    deleteRequest(url);
  }

  function deleteRequest(url) {
    $.ajax({
        url: url,
        type: 'DELETE',
        data: {'topic': topic},
        success: function(res) {
            console.log(res);
            alert(res);
            location.reload();
        },
        error: function(data){
            alert("fail " + data);

        }
    });
  }

</script>

<!-- Page Content -->
<div class="container">

  <!-- Page Heading -->
  <h1 class="my-4"><%- title %><small></small></h1>

  <div class="row">
    <% videos.forEach(function (video) { %>
    <div class="col-lg-3 col-md-4 col-sm-6 portfolio-item">
      <div class="card h-100">
        <a href="#"><img class="card-img-top" src="http://placehold.it/700x400" alt=""></a>
        <div class="card-body">
          <h4 class="card-title"><a href="#"><%- video.title %></a></h4>
          <h6 class="card-subtitle mb-2 text-muted"><%- video.owner.username %></h6>
          <p class="card-text"><%- video.description %></p>
          <a href="<%- baseUrl %>videos/delete/<%- video._id %>" class="card-link">Delete</a>
          <a href="<%- baseUrl %>videos/edit?id=<%- video._id %>" class="card-link">Edit</a>
          <a href="<%- baseUrl %>videos/me/share?id=<%- video._id %>" class="card-link">Share</a>
        </div>
      </div>
    </div>
    <% }); %>
  </div>
  <!-- /.row -->

  <!-- Pagination -->
  <ul class="pagination justify-content-center">
    <li class="page-item">
      <a class="page-link" href="#" aria-label="Previous">
        <span aria-hidden="true">&laquo;</span>
        <span class="sr-only">Previous</span>
      </a>
    </li>
    <li class="page-item">
      <a class="page-link" href="#">1</a></li>
    <li class="page-item">
      <a class="page-link" href="#">2</a></li>
    <li class="page-item">
      <a class="page-link" href="#">3</a></li>
    <li class="page-item">
      <a class="page-link" href="#" aria-label="Next">
        <span aria-hidden="true">&raquo;</span>
        <span class="sr-only">Next</span>
      </a>
    </li>
  </ul>

</div>
<!-- /.container -->


<% include footer %>
